/**
 * Shipping is free on every order. Function kept for call-site compatibility.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function calculateShipping(_subtotalPaise: number): number {
  return 0;
}
